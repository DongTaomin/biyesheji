# 基于大语言模型的交互式小说重构平台的设计与实现

## 项目重点

### 1. 多模型统一接入架构

项目自研了一套完整的 AI 提供商管理系统（`src/lib/ai/`），是整个平台的核心基础设施。采用**策略模式**，`GeminiProvider` 与 `OpenAICompatibleProvider` 共同实现统一的 `AIProvider` 接口，支持 Gemini、OpenAI、DeepSeek、Moonshot、本地 Ollama 等主流大模型的无缝切换。`ensureProviderForCall()` 实现智能路由，当 Gemini 提供商配置了非 Google 官方地址时自动切换至 OpenAI 兼容适配器。`AIConfigManager` 基于 localStorage 实现提供商配置的增删改查、导入导出与版本管理，体现了**面向接口编程**与**开闭原则**的工程实践。

### 2. 上下文工程（Context Engineering）

续写功能的核心技术亮点在于结构化的多层上下文组装（`Editor.tsx:834-865`）：

- **第一层**：用户自定义 AI 人设（写作风格、角色扮演指令）
- **第二层**：角色卡设定（只注入 `enabled=true` 的角色，按需过滤）
- **第三层**：关键词触发的世界书（仅注入当前章节文本中出现对应关键词的条目，动态裁剪上下文、节省 token）
- **第四层**：用户手动勾选的章节内容，并支持"章节全文 vs AI 摘要"二选一的**上下文压缩**策略

这种分层注入机制既保障了长文一致性，又通过关键词触发和摘要压缩控制了上下文长度，是提示词工程与上下文工程的综合应用。

### 3. 流式输出与实时交互体验

浏览器通过 `AsyncGenerator` 直接消费来自 AI 服务商的 SSE 流，每收到一个 `chunk` 即触发 React 状态更新，在编辑器中实现打字机效果。续写内容以"分界线"追加方式插入，支持一键撤回，降低用户试错成本。整个链路涉及**异步生成器、流式 HTTP、React 并发渲染**的协同设计。

---

## 项目难点

### 1. 长文本一致性问题

小说续写的核心挑战在于：LLM 上下文窗口有限，跨章节的人物性格、情节逻辑、世界观规则难以保持一致。本项目通过"角色卡 + 世界书关键词触发 + 章节摘要压缩 + 手动上下文选择"的组合策略加以缓解，本质上是在**上下文长度与信息完整性之间做工程权衡**，需要在论文中阐明选择该方案的理论依据与实验依据。

### 2. 多提供商兼容性

不同 LLM 的 API 格式、参数名、认证方式、错误码各不相同。`OpenAICompatibleProvider` 需兼容 OpenAI / DeepSeek / Moonshot / Ollama 等，`GeminiProvider` 处理 Google 原生格式，同时配置管理器的验证逻辑对所有类型均适用。尤其是"Gemini 配置指向 OpenAI 兼容聚合服务"的动态路由切换，是兼容性设计的典型难点。

### 3. 前端直连 AI 的架构权衡

用户 API Key 存于 localStorage，浏览器直连 AI 服务商——这是**安全性与架构复杂度之间的工程决策**。该架构省去了后端代理层，降低了系统延迟与部署复杂度，但存在密钥暴露风险，需要在论文中对比两种架构方案并说明在毕设场景下的合理性与生产环境的改进方向。

### 4. 豆包生图的非标协议接入

生图功能通过逆向豆包网页版私有 SSE 协议实现，需解析 `event_type`、`content_type`、`image.status` 等无文档的私有字段，并设计轮询兜底机制（SSE 未返回图片时转轮询 `/message_node_info`）。协议完全依赖抓包分析，无稳定性保障，属于**系统集成层面的工程难点**。

### 5. AI 检测多级兜底的服务可用性设计

第三方 AI 检测服务（ailv.run）下线后，重新设计了"Winston AI → ZeroGPT → 项目内置 LLM 自判断"的三级降级策略，涉及**异构 API 响应格式归一化**（各服务字段名、数值范围不一致）与**服务可用性兜底**（额度耗尽、网络不通、协议格式异常均触发回退），体现了健壮性设计的工程思路。

---

# 未来工作

## 1. 引入检索增强生成（RAG）

当前系统通过用户手动勾选章节来注入上下文，存在两个局限：一是依赖用户主动操作，二是当书籍篇幅较长时相关章节难以人工定位。未来可引入向量数据库（如 Faiss、Chroma），对书籍的所有章节、角色卡、世界书条目进行嵌入索引，在续写时自动检索语义最相关的片段注入上下文，实现更精准的长文一致性保障，无需用户手动管理上下文。

## 2. 结构化输出与输出验证框架（Output Harness）

当前 AI 生成的续写内容为自由文本，缺乏格式约束与质量验证。未来可引入结构化输出机制，要求模型按照固定 Schema 返回（如情节标签、人物出场、情绪弧线等元数据），并在输出不满足约束时自动重试或降级。这将为后续的情节一致性检测、风格分析等功能提供结构化数据基础。

## 3. AI 检测与降重的闭环优化

当前 AI 检测采用"Winston AI → ZeroGPT → LLM 自判断"的三级兜底策略，但各服务对中文内容的检测精度存在差异。未来可收集用户反馈，建立本地标注数据集，训练或微调一个面向中文网络小说的专用检测模型，同时结合检测结果驱动针对性的人化润色建议，形成"检测—改写—再检测"的完整闭环。

## 4. 多 Agent 协作写作

当前续写是单轮生成，由用户提供指令后模型一次性输出。未来可探索多 Agent 协作架构：设计"策划 Agent"负责生成情节大纲、"写作 Agent"负责文本扩写、"审校 Agent"负责一致性检查，三者通过结构化消息协作完成一次高质量的章节生成，减少用户干预的同时提升内容质量。

## 5. API 密钥后端托管与安全加固

当前用户 API Key 存储在浏览器 localStorage，浏览器直连 AI 服务商。这一架构在毕设演示场景下可行，但存在密钥泄露风险。未来应将 API Key 加密存储于后端数据库，所有 AI 调用通过服务端代理转发，并引入访问频率限制与用量统计，以支持多租户生产环境部署。

## 6. 生图接入官方 API

当前生图功能通过逆向豆包网页版私有接口实现，依赖用户 Cookie 鉴权，协议稳定性差且不符合服务条款。未来应替换为火山方舟官方图像生成 API 或其他开放图像生成服务（如 Stable Diffusion、DALL·E），在合规的前提下提供更稳定、可扩展的生图能力。

## 7. 协同创作与社区生态

当前平台为单用户本地模式（IndexedDB 存储）。未来可引入云端存储与多用户协同编辑，支持多位作者共同创作同一部小说；同时扩展社区功能，允许用户分享角色卡、世界书模板、提示词配置，形成围绕 AI 辅助写作的创作者生态。
