这个项目的设计思路可以概括为：“一个闭环、两大引擎、全栈联动”。它旨在打破网文创作中“素材搜集、逻辑规划、AI  
 协同”相互脱节的现状，构建一个从“输入（阅读/分析）”到“输出（AI 续写/生成）”的完整生产线。

以下是该项目核心设计思路的详细拆解：

### 1. 核心哲学：从“工具”进化为“生态”

项目不再将阅读器和编辑器视为孤立的个体，而是通过 “导入创作” 这一逻辑，将外部海量网文资源转化为 AI 的学习样本。

• 设计逻辑：用户在爬虫引擎支撑的书城中发现好书 -> 一键拆解细纲、分析风格 -> 将风格注入大模型上下文 -> 在编辑器中进行个性化创作。

---

### 2. 大模型集成设计：统一 AI 中台架构

为了解决不同大模型厂商 API 协议碎片化、切换成本高的问题，项目采用了一套解耦适配层设计。

• 统一适配层 (UnifiedAIClient)：  
 • 接口标准化：通过定义 AIProvider 接口，将 Google Gemini、OpenAI、Claude、DeepSeek 等异构 API  
 归一化。无论底层调用谁，上层业务逻辑（如续写、校对）代码完全一致。  
 • 智能协议路由：设计了动态校正机制。例如，当检测到用户填写的 Gemini 地址是国内代理地址时，系统会自动从官方 SDK 模式切换为兼容 OpenAI 的标准
HTTP 模式，极大提升了网络鲁棒性。  
 • 上下文增强策略：  
 • 结构化注入：AI 并不是“盲目”续写。设计思路中包含了  
 “上下文自动组装引擎”，它会自动从角色卡中提取人物性格，从世界书中提取设定关键词，从历史章节中提取剧情，动态拼接成高价值的 Prompt 喂给大模型。

---

### 3. 爬虫引擎设计：基于沙箱的动态规则解释器

项目没有采用硬编码的“死抓取”逻辑，而是设计了一套高度兼容、可编程的解析引擎。

• 协议兼容设计：参考了移动端成熟的“阅读 (Legado)”协议，支持以 JSON 格式配置书源。  
 • 混合解析模型：  
 • 静态 + 动态联动：支持 CSS 选择器定位 DOM，同时嵌入 @js: 标签。  
 • JS 沙箱执行环境：核心设计在于 createSandbox 运行时。它在 Node.js 环境中模拟了 Android 端的各种 API（如 java.ajax, java.md5,  
 cookie.getCookie），并使用 vm2 进行物理隔离。  
 • 自研 JSONPath：为了应对现代网站频繁使用的 JSON API，自研了轻量级解析器，支持递归搜索和条件过滤，确保了对 SPA（单页应用）网站的解析能力。

---

### 4. 前端架构设计：全栈响应式与离线优先

• 渲染策略：基于 Next.js 15 的混合渲染。书城列表采用 SSR/ISR 保证 SEO 和首屏速度；编辑器模块采用 use client 保证极速的交互响应。  
 • 存储冗余设计：  
 • 轻量配置：用户 API Key、书源配置存放在 LocalStorage。  
 • 海量数据：小说正文、缓存的书城数据存放在 IndexedDB，确保在断网情况下依然可以进行流畅的离线写作。  
 • 交互设计：采用 SSE (Server-Sent Events) 协议。AI 的校对、续写、生图过程均是流式反馈，避免了长时间加载带来的卡顿感。

### 5. 总结：系统架构图景

1. 底层驱动：爬虫解析引擎（解决“素材从哪来”）。
2. 核心大脑：多模型统一客户端（解决“AI 怎么用”）。
3. 业务载体：上下文感知编辑器（解决“内容怎么产出”）。
